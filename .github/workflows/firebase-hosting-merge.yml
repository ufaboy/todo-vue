# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2.2.2
        with:
          version: 7
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      # Устанавливаем зависимости для сборки
      - name: Install Dependencies
        run: pnpm install
      # Устанавливаем env
      - name: Install env
        run: |
          echo "VITE_APP_FB_API_KEY="${{ secrets.FB_API_KEY }} > .env
          echo "VITE_APP_FB_AUTH_DOMAIN="${{ secrets.FB_AUTH_DOMAIN }} > .env
          echo "VITE_APP_FB_PROJECT_ID="${{ secrets.FB_PROJECT_ID }} > .env
          echo "VITE_APP_FB_STORAGE_BUCKET="${{ secrets.FB_STORAGE_BUCKET }} > .env
          echo "VITE_APP_FB_MESSAGING_SENDER_ID="${{ secrets.FB_MESSAGING_SENDER_ID }} > .env
          echo "VITE_APP_FB_APP_ID="${{ secrets.FB_APP_ID }} > .env
      # Собираем приложение
      - name: Build Appliction
        run: pnpm build
      # Доставляем собранное приложение на сервер
      - name: Deploy to Server
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TODO_F2FE4 }}'
          channelId: live
          projectId: todo-f2fe4
